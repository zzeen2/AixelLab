import React from 'react'
import styled from 'styled-components'

const LogoText = styled.h1`
    font-size: 1.5rem;
    font-weight: bold;
    color: white;
    margin: 0;
    `

const Logo = () => {
    return (
        <LogoText>Aixel</LogoText>
    )
}

export default Logo
