import React from "react";
import {MainTemplate} from "../templates";

const MainPage = () => {
    return (
        <MainTemplate>
        <div style={{ color: "#fff" }}>
            <h1>🎨 AixelLab</h1>
            <p>create ur nft</p>
        </div>
        </MainTemplate>
    );
};

export default MainPage;
