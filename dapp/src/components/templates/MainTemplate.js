import React from 'react';
import {CarouselSection} from '../organism';

const MainTemplate = () => {
    return (
        <main>
            <CarouselSection />
        </main>
  );
};

export default MainTemplate;
